int main() {
	
    int n,i,sum=0,mod;
   scanf("%d", &n);
    for(i=0;i<5;i++) // for 5 digit, 5 times loop will execute starting from 0
    { 
        mod=(n%10); 
        n=(n/10);
        sum+=mod;
     }
    printf("%d",sum);
    return 0;
}
